- 使用rg,而不是find
- 使用简体中文,除了专有名词
- git commit msg应该类似`feat:`/`fix:`/`docs:`/`rm:`/`migrate:`/`opt.:`等等常见开头
- 除非用户要求,否则不能force push
- 如果已经在对应目录, 就不要cd
- 隐私/密钥不能出现在上下文,应该通过环境变量/pipe传递
- 所有的迁移兼容/残留项应该标记TODO, 提示待删除
- 永远是正确性优先,有错及时改;其次实现需要优雅,不要缝缝补补
- 安全性很重要,禁止偷懒
